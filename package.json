{
  "name": "sistema-setec-energia",
  "version": "1.0.0",
  "private": true,
  "description": "Sistema de Gestão e Monitoramento Solar SETEC ENERGIA",
  "scripts": {
    "postinstall": "cd backend && npx prisma generate",
    "dev": "npx -y concurrently -n \"BACKEND,FRONTEND\" -c \"blue,green\" \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "build": "cd backend && npx prisma generate && npm run build && cd ../frontend && npm run build",
    "install:all": "npm install --prefix backend && npm install --prefix frontend"
  }
}
